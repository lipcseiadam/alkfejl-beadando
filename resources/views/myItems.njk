{% extends 'layout' %}

{% block content %}

<h2>Kikölcsönzött eszközök</h2>

{% extends 'layout' %}


<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Név</th>
      <th>Mennyiség</th>
      <th>Kategória</th>
      <th>Dátum</th>
    </tr>
  </thead>
  <tbody>
  {% for category in categories %}
    {% for item in rents %}
    {%if item.user_id == currentUser.id %}
    {% if item.category_id == category.id %}
      <tr class="warning">  
      <td>{{ item.item_name }} </td>
      <td>{{item.quantity}}</td>
      <td>{{ category.name }}</td>  
      <td>{{ item.created_at }}</td> 
      </tr>
      {% endif %}
    {% endif %}
    {% endfor %}
  {% endfor %}
{% endblock %}