{% extends 'layout' %}

{% block content %}

<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>#</th>
      <th>Név</th>
      <th>Szobaszám</th>
      <th>Email</th>
    </tr>
  </thead>
  {% for user in users %}
  <tbody>
    <tr>
      <td>{{user.id}}</td>
      <td>{{user.name}}</td>
      <td>{{user.roomnumber}}</td>
      <td>{{user.email}}</td>
    </tr>
    <tr>
  </tbody>
  {% endfor %}
</table> 


{% endblock %}